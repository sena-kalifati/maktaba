CREATE TABLE [dbo].[LibrarianTbl] (
    [LibId]        INT           NOT NULL,
    [LibName]      VARCHAR (50)  NOT NULL,
    [LibePassword] NVARCHAR (50) NOT NULL,
    [LipePhone]    VARCHAR (50)  NOT NULL,
    PRIMARY KEY CLUSTERED ([LibId] ASC)
);

